// Coeficientes da equação de segundo grau (altere conforme necessário)
const a = 5;
const b = 5;
const c = 1;

// Fórmula para calcular o valor do delta
const delta = b ** 2 - 4 * a * c;

// Exibir o resultado no console
console.log(`Para a equação ${a}x^2 + ${b}x + ${c} = 0:`);
console.log(`O valor de delta é: ${delta}`);
