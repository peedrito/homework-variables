// Informação do clima
const Fahrenheit = 73;

// Sistema de conversão
const conversao = ((Fahrenheit - 32) * 5/9).toFixed();

// Imprime no console
console.log(`Hoje o clima está ${Fahrenheit} °F, convetendo para °C o clima atual é ${conversao}.`)
